"""Package marker for custom_components to help static type checkers (mypy).

This file intentionally empty; it documents that `custom_components` is a package
so mypy treats modules inside as part of that package and avoids duplicate
module name detection when running in different import contexts.
"""
