# /plan

Use the planner agent to draft an implementation plan before coding.

Steps:

1. Gather context: issue/goal, HA version target, affected domains.
2. Run planner: produce file list, steps, risks, and test plan (pytest,
   fixtures).
3. Review and adjust scope; keep diffs minimal.
