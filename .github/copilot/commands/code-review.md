# /code-review

Run the code-reviewer agent on the diff.

Include:
- Async/HA correctness, entity metadata, unload/reload.
- Tests present and deterministic; coverage impact.
- Secrets/logging hygiene; minimal diffs.
Provide findings by severity with fixes.
